name: Process books/progress updates on master

on:
  push:
    branches:
      - master
    paths:
      - data/books.md
      - data/progress.md

permissions:
  contents: read

jobs:
  process-master-update:
    runs-on: ubuntu-latest

    steps:
      - name: Acknowledge direct master update
        run: echo "Data files updated directly on master."
